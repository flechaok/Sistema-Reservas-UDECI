# Sistema de Reservas - Django + Java + Docker 🧩

## 🚀 Cómo ejecutar
```bash
docker compose up --build
API Java → http://localhost:8080/reservas

Django (UI) → http://localhost:8000/reservas